{
  "name": "@unidocs/cloudflare-cas",
  "version": "0.1.0",
  "description": "Tenant-scoped CAS worker for UniDocs",
  "type": "module",
  "main": "./src/worker.ts",
  "types": "./src/worker.ts",
  "exports": {
    ".": {
      "types": "./src/worker.ts",
      "import": "./src/worker.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "clean": "rimraf --glob dist \"*.tsbuildinfo\"",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@unidocs/cas-server-common": "workspace:*",
    "@unidocs/protocol": "workspace:*",
    "@unidocs/protocol-cas": "workspace:*",
    "@unidocs/svalue-codec": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250817.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.20.0"
  },
  "publishConfig": {
    "main": "./dist/worker.js",
    "types": "./dist/worker.d.ts",
    "exports": {
      ".": {
        "types": "./dist/worker.d.ts",
        "import": "./dist/worker.js"
      }
    }
  }
}