description: "History returns 5 deltas: create, setContent, append, multi-op, rollback"
steps:
  - type: exec
    command: curl -s http://localhost:8787/md-e2e-001/history
    assert:
      type: jsonata
      expression: "success = true and $count(data) = 5"
