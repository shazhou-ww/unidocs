# 复制成 .env.azure 并填上你的 key,本地 Azure 栈的 PSD Operator(聊天框
# agent)才会工作。.env.azure 被 .gitignore 忽略;这份 .example 入库。
#
# `pnpm dev unidocs-azure` 会自己读根目录的 .env.azure
# (stacks/unidocs-azure/local/dev.mjs → dev-env.mjs),不需要手动 source。
# shell 里已有的同名变量优先,所以 `LLM_MODEL=xxx pnpm dev unidocs-azure`
# 这种一次性覆盖仍然有效。想手动加载也可以:`set -a; source .env.azure; set +a`。
#
# 取值顺序见 packages/doctype-server-common/src/agent/providers/anthropic.ts:
#   LLM_BASE_URL || ANTHROPIC_API_BASE   (裸 host 和整条 /v1/messages 都接受)
#   LLM_API_KEY  || ANTHROPIC_API_KEY
#   LLM_MODEL    || ANTHROPIC_MODELS[0]  (默认 claude-opus-5)

LLM_API_KEY=sk-ant-...
# Optional — defaults shown:
# LLM_BASE_URL=https://api.anthropic.com
# LLM_MODEL=claude-opus-5

# 图像编辑模型（editPixels）。不配就没有 editPixels 工具,层内像素编辑不可用
# (packages/azure-psd/src/main.ts 按 key 是否存在条件化注入 editor)。
# DashScope 控制台拿 key：https://bailian.console.aliyun.com/
IMAGE_EDIT_API_KEY=sk-...
# Optional — defaults shown:
# IMAGE_EDIT_MODEL=qwen-image-edit-plus
# IMAGE_EDIT_BASE_URL=https://dashscope.aliyuncs.com
