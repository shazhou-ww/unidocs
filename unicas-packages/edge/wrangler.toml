name = "unidocs-cas-edge"
main = "dist/worker.js"
compatibility_date = "2025-08-17"

# Sole public Worker on the CAS service hostname: /stacks + /admin dispatch.
# Backing Workers remain private (no independent public routes).
[[routes]]
pattern = "unicas.shazhou.work/*"
zone_name = "shazhou.work"

[[services]]
binding = "CAS_TENANT_SERVICE"
service = "unidocs-cas-server-cloudflare"

[[services]]
binding = "CAS_ADMIN_SERVICE"
service = "unidocs-cas-admin-webui"

[[services]]
binding = "CAS_MCP_SERVICE"
service = "unidocs-cas-control-plane-mcp"

[vars]
CAS_EDGE_COMPAT_VERSION = "0"
CAS_PUBLIC_ORIGIN = "https://unicas.shazhou.work"
