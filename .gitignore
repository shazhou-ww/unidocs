node_modules/
dist/
.dwr/
.wrangler/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example
!.env.azure.example
.dev.vars
!.dev.vars.example
.treespec-output/
.artifacts/
.wrangler/
.azure-runtime/
.pnpm-store/

# Build-time source modules containing bundled frontend assets
/packages/azure-gateway/src/web-assets.generated.ts
/packages/cloudflare-gateway/src/ui-assets.generated.ts
/unicas-packages/service-cloudflare/src/admin-bff/ui-assets.generated.ts

# 本地生成的部署私钥,永不入库
.secrets/

# local dev server logs (never commit — they contain issued capability tokens)
.dev-*.log

# subagent-driven-development 的工作区（台账、简报、评审包），不入库
.superpowers/

# 部署者自备的**全量**字体二进制。一套中文字体 5-20 MB,进 git 就永远留在历史里
# (裁定 R19)。R19 于 2026-09-08 收窄为"只许提交有明确公开字表依据的子集,单文件
# 不超过约 3 MiB",所以 packages/fonts-builtin/fonts/ 下那两套随包发行的默认字体
# 是**有意提交**的,不在这条规则的射程内。
# 只挡这一个目录,不挡全仓库的 *.ttf —— 测试要用小字体
# (packages/doctype-psd/tests 现在是在内存里现造的,将来可能落成 fixture)。
/fonts/
